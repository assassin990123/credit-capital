{
  "dependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start": "lerna run start",
    "compile": "lerna run --scope {solidity} compile",
    "test": "lerna run --scope {solidity} test"
  },
  "name": "credit-capital"
}
