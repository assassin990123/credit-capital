export interface DashboardState {
  dailyEarnings: number | null;
  tvl: number | null;
  totalCAPL: number | null,
  userCAPL: number | null,
  stakedBalance: number | null,
  walletAddress: string | null,
  usdcBalance: number | null,
}